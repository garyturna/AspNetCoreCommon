# Gary's Diner Application

## Projects

### GarysDinerDB
SQL Server database project.

- Creates all tables and stored procedures
- Post deployment script to insert initial seed data

### DataLibrary
Class library for database access.

- Connects to SQL Server using Dapper
- Exposes data access methods via interfaces
- Keeps database logic separate from the UI layer
